Description: Drop request for qt 5.4, works fine with 5.3 too.
Author: Mateusz Łukasik <mati75@linuxmint.pl>

--- a/qt5ct.pri
+++ b/qt5ct.pri
@@ -6,14 +6,6 @@ RCC_DIR=./.build/rcc
 
 CONFIG += c++11
 
-!isEqual (QT_MAJOR_VERSION, 5) {
-  error("Use Qt 5.4.0 or higher.")
-}
-
-!greaterThan(QT_MINOR_VERSION, 3) {
-  error("Use Qt 5.4.0 or higher.")
-}
-
 #Install paths
 unix {
   isEmpty(PREFIX) {
